version: 1
applications:
  - backend:
      phases:
        build:
          commands:
            - pip install -r requirements.txt
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
